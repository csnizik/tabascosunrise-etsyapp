# ===========================================
# TabascoSunrise Etsy App - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values
# cp .env.example .env.local

# -------------------------------------------
# Etsy OAuth Configuration
# -------------------------------------------
# Get these from the Etsy Developer Portal: https://www.etsy.com/developers/your-apps
# 1. Create a new app or select an existing one
# 2. Find your API Key (Keystring) in the app settings
# 3. Add your redirect URI to the allowed callback URLs

ETSY_API_KEY=your_api_key_here
ETSY_REDIRECT_URI=http://localhost:3000/api/auth/etsy/callback
ETSY_SCOPES=listings_r shops_r

# -------------------------------------------
# Vercel Edge Config
# -------------------------------------------
# Set up Edge Config in your Vercel Dashboard:
# 1. Go to your Vercel project → Storage → Create Database → Edge Config
# 2. After creation, go to the Edge Config → Settings tab
# 
# EDGE_CONFIG: Connection string (auto-set by Vercel when linked to project)
#   Format: https://edge-config.vercel.com/ecfg_xxxxx?token=xxxxx
#   Find it in: Edge Config → Settings → Connection String
#
# EDGE_CONFIG_ID: The Edge Config ID
#   Format: ecfg_xxxxx
#   Find it in: Edge Config → Settings → Edge Config ID
#
# EDGE_CONFIG_TOKEN: Vercel API token with Edge Config access
#   Create at: https://vercel.com/account/tokens
#   Required scope: Full access or Edge Config read/write

EDGE_CONFIG=https://edge-config.vercel.com/ecfg_xxxxx?token=xxxxx
EDGE_CONFIG_ID=ecfg_xxxxx
EDGE_CONFIG_TOKEN=vercel_token_xxxxx

# -------------------------------------------
# Vercel Cron Jobs
# -------------------------------------------
# Secret for authenticating cron job requests
# Generate a random secret: openssl rand -base64 32
# Vercel sends this as: Authorization: Bearer <CRON_SECRET>

CRON_SECRET=your_cron_secret_here

# -------------------------------------------
# Optional Configuration
# -------------------------------------------
# Enable logging in production (logs are enabled by default in development)
# LOG_ENABLED=true
